import CardWrapper from "@/components/custom/card-wrapper";

const Client = () => {
  return (
    <CardWrapper
      title="Signin to Rona"
      description="Use your login credentials to login to your account"
    >
      Client
    </CardWrapper>
  );
};

export default Client;
