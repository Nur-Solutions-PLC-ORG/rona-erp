import LauncherGrid from "@/modules/workspace/components/launcher-grid";
import { Metadata } from "next";

export const metadata: Metadata = { title: "Apps | Rona ERP" };

const Page = () => {
  return <LauncherGrid />;
};

export default Page;
