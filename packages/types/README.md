# Package: @rona/types

This package contains all the types for the rona-erp monorepo.

## Rules

- Use PascalCase for all types: Capitalize the first letter of every word (eg. UserRole)
- Name your types as a single unit (eg. User, Employee)
- Avoid matching names with built-in primitives (eg. no String, no Number)
